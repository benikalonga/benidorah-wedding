export default function OfflinePage() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center bg-ivory px-6 text-center">
      <h1 className="section-title text-3xl text-onyx">You're offline</h1>
      <p className="mt-3 max-w-sm text-charcoal">
        Beni &amp; Dorah's wedding site needs a connection to load fresh content. Reconnect and try again — anything
        you already opened should still be cached.
      </p>
    </main>
  );
}
